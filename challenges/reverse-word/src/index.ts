/*
* #=========================================#
* | Write a function that reverses any word | 
* #=========================================#
*/

function reverseWord(word: string): string {
  return word.split('').reverse().join();
}
 